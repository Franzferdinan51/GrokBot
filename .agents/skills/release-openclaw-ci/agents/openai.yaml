interface:
  display_name: "GrokBot Release CI"
  short_description: "Verify and debug GrokBot release validation runs"
  default_prompt: "Use $release-grokbot-ci to validate one frozen release SHA with one active parent run, recover only the smallest failed lane, and stop on a precise external blocker."
