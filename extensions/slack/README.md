# GrokBot Slack

Official GrokBot channel plugin for Slack channels, DMs, commands, and app events.

Install from GrokBot:

```bash
grokbot plugin add @grokbot/slack
```

Configure the Slack app credentials and allowed workspaces/channels in GrokBot. The plugin lets agents receive Slack events and reply through the configured Slack app.
