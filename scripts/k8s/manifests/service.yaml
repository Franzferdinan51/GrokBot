apiVersion: v1
kind: Service
metadata:
  name: grokbot
  labels:
    app: grokbot
spec:
  type: ClusterIP
  selector:
    app: grokbot
  ports:
    - name: gateway
      port: 18789
      targetPort: 18789
      protocol: TCP
